import { Problem } from '../types';

export const problems: Problem[] = [
  // ==================== 哈希表类题目 ====================
  {
    id: 'two-sum',
    title: '两数之和',
    titleEn: 'Two Sum',
    category: 'hash-table',
    difficulty: 'easy',
    description: '给定一个整数数组 nums 和一个整数目标值 target，请你在该数组中找出和为目标值 target 的那两个整数，并返回它们的数组下标。\n\n你可以假设每种输入只会对应一个答案。但是，数组中同一个元素在答案里不能重复出现。',
    examples: [
      { input: 'nums = [2,7,11,15], target = 9', output: '[0,1]', explanation: '因为 nums[0] + nums[1] == 9 ，返回 [0, 1] 。' },
      { input: 'nums = [3,2,4], target = 6', output: '[1,2]' },
      { input: 'nums = [3,3], target = 6', output: '[0,1]' }
    ],
    thinkingGuide: [
      { step: 1, question: '🤔 暴力解法怎么做？', hint: '两层循环枚举', answer: '两层 for 循环枚举所有可能的组合，时间复杂度 O(n²)。' },
      { step: 2, question: '⚡️ 如何优化查找速度？', hint: '用空间换时间', answer: '使用哈希表（Map）记录已经遍历过的数字及其索引。' },
      { step: 3, question: '🔍 每次遍历需要查找什么？', hint: 'target - current', answer: '对于当前数字 x，我们需要查找哈希表中是否存在 target - x。' }
    ],
    codeSteps: [
      {
        title: '初始化哈希表',
        description: '创建一个 Map 用于存储数值到索引的映射',
        code: 'Map<Integer, Integer> map = new HashMap<>();',
        explanation: '哈希表能提供 O(1) 的查找效率，这是优化的关键。'
      },
      {
        title: '遍历数组',
        description: '一次遍历，边查边存',
        code: `for (int i = 0; i < nums.length; i++) {
    int complement = target - nums[i];
    // 检查补数是否存在
    if (map.containsKey(complement)) {
        return new int[] { map.get(complement), i };
    }
    // 存入当前数
    map.put(nums[i], i);
}`,
        explanation: '在遍历的同时检查补数，如果存在直接返回，否则将当前数存入哈希表供后续查找。'
      }
    ],
    interview: {
      approach: '使用哈希表（HashMap）来存储已经遍历过的数字和它的索引。这样我们可以在 O(1) 的时间内判断 target - nums[i] 是否在数组中。',
      timeComplexity: 'O(n)，我们只遍历了包含 n 个元素的列表一次。在表中进行的每次查找只花费 O(1) 的时间。',
      spaceComplexity: 'O(n)，所需的额外空间取决于哈希表中存储的元素数量，该表最多需要存储 n 个元素。',
      followUp: [
        { question: '如果数组是有序的，怎么做？', answer: '可以使用双指针法，左指针指向开头，右指针指向结尾，根据和的大小移动指针。时间复杂度 O(n)，空间复杂度 O(1)。' },
        { question: '如果需要返回所有不重复的组合呢？', answer: '先排序，然后使用双指针，注意跳过重复元素（类似三数之和）。' }
      ]
    }
  },
  {
    id: 'group-anagrams',
    title: '字母异位词分组',
    titleEn: 'Group Anagrams',
    category: 'hash-table',
    difficulty: 'medium',
    description: '给你一个字符串数组，请你将 字母异位词 组合在一起。可以按任意顺序返回结果列表。\n\n字母异位词 是由重新排列源单词的字母得到的一个新单词，所有源单词中的字母通常恰好只用一次。',
    examples: [
      { input: 'strs = ["eat", "tea", "tan", "ate", "nat", "bat"]', output: '[["bat"],["nat","tan"],["ate","eat","tea"]]' },
      { input: 'strs = [""]', output: '[[""]]' },
      { input: 'strs = ["a"]', output: '[["a"]]' }
    ],
    thinkingGuide: [
      { step: 1, question: '🤔 什么是字母异位词？', hint: '字符相同，顺序不同', answer: '两个字符串包含的字符种类和数量完全相同，只是排列顺序不同。' },
      { step: 2, question: '🔑 如何判断两个字符串是异位词？', hint: '排序或计数', answer: '方法一：对字符串排序，排序后相同的即为异位词。方法二：统计每个字符出现的次数。' },
      { step: 3, question: '📦 怎么把它们分到一组？', hint: '哈希表分类', answer: '使用哈希表，以“特征”作为 key，以字符串列表作为 value。特征可以是排序后的字符串，也可以是字符计数数组。' }
    ],
    codeSteps: [
      {
        title: '初始化哈希表',
        description: 'Key是排序后的字符串，Value是原字符串列表',
        code: 'Map<String, List<String>> map = new HashMap<>();',
        explanation: '利用 Map 的键值对特性进行分组。'
      },
      {
        title: '遍历并分组',
        description: '对每个字符串生成 Key 并存入 Map',
        code: `for (String s : strs) {
    char[] chars = s.toCharArray();
    Arrays.sort(chars);
    String key = new String(chars);
    
    if (!map.containsKey(key)) {
        map.put(key, new ArrayList<>());
    }
    map.get(key).add(s);
}`,
        explanation: '将字符串排序作为 Key，这样所有异位词都会有相同的 Key，从而被分到同一个 List 中。'
      },
      {
        title: '返回结果',
        description: '将 Map 的 Values 转为 List',
        code: 'return new ArrayList<>(map.values());',
        explanation: '题目要求返回列表的列表，直接取 Map 的 values 即可。'
      }
    ],
    interview: {
      approach: '使用哈希表分组。对于每个字符串，将其排序后作为 key，原字符串作为 value 加入对应的列表中。',
      timeComplexity: 'O(NKlogK)，其中 N 是字符串数量，K 是字符串的最大长度。排序需要 O(KlogK)。',
      spaceComplexity: 'O(NK)，需要存储所有字符串。',
      followUp: [
        { question: '如果不允许排序，怎么做？', answer: '可以使用字符计数数组（长度26）作为 key。将数组转为字符串或使用自定义对象作为 Map 的 key。时间复杂度优化为 O(NK)。' }
      ]
    }
  },
  {
    id: 'longest-consecutive-sequence',
    title: '最长连续序列',
    titleEn: 'Longest Consecutive Sequence',
    category: 'hash-table',
    difficulty: 'medium',
    description: '给定一个未排序的整数数组 nums ，找出数字连续的最长序列（不要求序列元素在原数组中连续）的长度。\n\n请你设计并实现时间复杂度为 O(n) 的算法解决此问题。',
    examples: [
      { input: 'nums = [100,4,200,1,3,2]', output: '4', explanation: '最长数字连续序列是 [1, 2, 3, 4]。它的长度为 4。' },
      { input: 'nums = [0,3,7,2,5,8,4,6,0,1]', output: '9' }
    ],
    thinkingGuide: [
      { step: 1, question: '🤔 要求 O(n) 意味着什么？', hint: '不能排序', answer: '通常排序需要 O(nlogn)，所以不能先排序。需要用哈希表来辅助查找。' },
      { step: 2, question: '🔍 如何找到序列的起点？', hint: '前一个数不存在', answer: '如果一个数 x，它的前一个数 x-1 不在数组中，那么 x 就是一个序列的起点。' },
      { step: 3, question: '📏 怎么计算长度？', hint: '不断查找 x+1', answer: '从起点 x 开始，不断查找 x+1, x+2... 是否存在，直到断开。' }
    ],
    codeSteps: [
      {
        title: '去重并存入哈希集合',
        description: '使用 Set 去重并支持 O(1) 查找',
        code: `Set<Integer> numSet = new HashSet<>();
for (int num : nums) {
    numSet.add(num);
}`,
        explanation: 'Set 既能去重，又能让我们快速判断一个数字是否存在。'
      },
      {
        title: '遍历寻找起点并计算长度',
        description: '只对“起点”进行延伸计算',
        code: `int longestStreak = 0;

for (int num : numSet) {
    // 只有当 num-1 不存在时，num 才是起点
    if (!numSet.contains(num - 1)) {
        int currentNum = num;
        int currentStreak = 1;

        // 向后寻找连续序列
        while (numSet.contains(currentNum + 1)) {
            currentNum += 1;
            currentStreak += 1;
        }

        longestStreak = Math.max(longestStreak, currentStreak);
    }
}`,
        explanation: '关键在于 `!numSet.contains(num - 1)` 这个判断，保证了每个序列只会被计算一次（从起点开始），从而保证总时间复杂度为 O(n)。'
      }
    ],
    interview: {
      approach: '使用 HashSet 存储所有数字。遍历 Set 中的每个数字，如果它是一个序列的起点（即 num-1 不在 Set 中），就向后寻找 num+1, num+2... 直到断开，更新最大长度。',
      timeComplexity: 'O(n)，虽然有 while 循环，但因为只有起点会触发 while，且每个数字最多被访问两次（一次在 for 中，一次在 while 中），所以总复杂度是线性的。',
      spaceComplexity: 'O(n)，需要一个 Set 来存储数组元素。',
      followUp: [
        { question: '并查集能做吗？', answer: '可以。将相邻的数字（x 和 x+1）合并到同一个集合，最后看最大的集合大小。但哈希表法代码更简洁。' }
      ]
    }
  },

  // ==================== 双指针类题目 ====================
  {
    id: 'move-zeroes',
    title: '移动零',
    titleEn: 'Move Zeroes',
    category: 'two-pointers',
    difficulty: 'easy',
    description: '给定一个数组 nums，编写一个函数将所有 0 移动到数组的末尾，同时保持非零元素的相对顺序。\n\n请注意 ，必须在不复制数组的情况下原地对数组进行操作。',
    examples: [
      { input: 'nums = [0,1,0,3,12]', output: '[1,3,12,0,0]' },
      { input: 'nums = [0]', output: '[0]' }
    ],
    thinkingGuide: [
      { step: 1, question: '🤔 怎么理解“保持非零元素相对顺序”？', hint: '像排队一样', answer: '非零元素之间的先后关系不能变，只是把中间的 0 抽走，后面的补上来。' },
      { step: 2, question: '👉 双指针怎么放？', hint: '快慢指针', answer: '使用两个指针，slow 指向当前已处理好的非零序列的尾部，fast 负责遍历数组寻找非零元素。' },
      { step: 3, question: '🔄 交换的逻辑是什么？', hint: '遇到非零就交换', answer: '当 fast 遇到非零元素时，将其放到 slow 的位置，然后 slow 向前移动一步。' }
    ],
    codeSteps: [
      {
        title: '双指针遍历',
        description: '一次遍历完成移动',
        code: `int slow = 0;
for (int fast = 0; fast < nums.length; fast++) {
    if (nums[fast] != 0) {
        // 优化：如果 slow 和 fast 指向同一位置，无需交换
        if (fast > slow) {
            nums[slow] = nums[fast];
            nums[fast] = 0;
        }
        slow++;
    }
}`,
        explanation: 'slow 指针维护的是[0, slow)区间内全是排好的非零元素。fast 指针负责探路，找到非零的就扔给 slow。'
      }
    ],
    interview: {
      approach: '使用快慢指针。快指针遍历数组，遇到非零元素就赋值给慢指针指向的位置，并将慢指针后移。如果快慢指针位置不同，赋值后将快指针位置置零（或者直接交换）。',
      timeComplexity: 'O(n)，只遍历一次数组。',
      spaceComplexity: 'O(1)，原地操作。',
      followUp: [
        { question: '如果要求减少写入次数呢？', answer: '当前的交换逻辑已经是最小写入次数了（每个非零元素只被写一次）。如果 0 很多，可以优化为不交换，直接覆盖，最后统一把 slow 之后的全填 0。' }
      ]
    }
  },
  {
    id: 'container-with-most-water',
    title: '盛最多水的容器',
    titleEn: 'Container With Most Water',
    category: 'two-pointers',
    difficulty: 'medium',
    description: '给定一个长度为 n 的整数数组 height 。有 n 条垂线，第 i 条线的两个端点是 (i, 0) 和 (i, height[i]) 。\n\n找出其中的两条线，使得它们与 x 轴共同构成的容器可以容纳最多的水。\n\n返回容器可以储存的最大水量。',
    examples: [
      { input: 'height = [1,8,6,2,5,4,8,3,7]', output: '49', explanation: '图中垂直线代表输入数组 [1,8,6,2,5,4,8,3,7]。在此情况下，容器能够容纳水（表示为蓝色部分）的最大值为 49。' },
      { input: 'height = [1,1]', output: '1' }
    ],
    thinkingGuide: [
      { step: 1, question: '📏 面积怎么算？', hint: '底 × 高', answer: '面积 = (right - left) * min(height[left], height[right])。' },
      { step: 2, question: '🤔 为什么要移动短板？', hint: '木桶效应', answer: '容器的高度取决于短板。如果移动长板，宽度变小，高度不可能变大（受限于短板），面积一定变小。只有移动短板，高度才有可能变大，面积才有可能增加。' },
      { step: 3, question: '👉 双指针怎么动？', hint: '向内收缩', answer: '左右指针分别指向两端，每次计算面积后，将高度较小的那一端的指针向内移动。' }
    ],
    codeSteps: [
      {
        title: '双指针向内收缩',
        description: '贪心策略：总是移动短板',
        code: `int left = 0, right = height.length - 1;
int maxArea = 0;

while (left < right) {
    // 计算当前面积
    int currentArea = Math.min(height[left], height[right]) * (right - left);
    maxArea = Math.max(maxArea, currentArea);

    // 移动短板
    if (height[left] < height[right]) {
        left++;
    } else {
        right--;
    }
}`,
        explanation: '从两端开始，每次放弃较短的那根线（因为它限制了高度），尝试寻找更长的线。'
      }
    ],
    interview: {
      approach: '双指针法。初始化左右指针在数组两端。每次计算面积，然后移动高度较小的那一端的指针。因为移动长板只会让面积变小（宽度减小，高度不变或更小），只有移动短板才有可能找到更高的线从而增加面积。',
      timeComplexity: 'O(n)，双指针总共遍历一次数组。',
      spaceComplexity: 'O(1)，只需要常数空间。',
      followUp: [
        { question: '证明为什么这样是对的？', answer: '反证法：假设最优解是 [i, j]，且我们通过移动短板错过了它。这与“移动长板一定变差”矛盾。' }
      ]
    }
  },
  {
    id: '3sum',
    title: '三数之和',
    titleEn: '3Sum',
    category: 'two-pointers',
    difficulty: 'medium',
    description: '给你一个整数数组 nums ，判断是否存在三元组 [nums[i], nums[j], nums[k]] 满足 i != j、i != k 且 j != k ，同时还满足 nums[i] + nums[j] + nums[k] == 0 。请\n\n你返回所有和为 0 且不重复的三元组。\n\n注意：答案中不可以包含重复的三元组。',
    examples: [
      { input: 'nums = [-1,0,1,2,-1,-4]', output: '[[-1,-1,2],[-1,0,1]]', explanation: '不同的三元组是 [-1,0,1] 和 [-1,-1,2] 。注意，输出的顺序和三元组的顺序并不重要。' },
      { input: 'nums = [0,1,1]', output: '[]' },
      { input: 'nums = [0,0,0]', output: '[[0,0,0]]' }
    ],
    thinkingGuide: [
      { step: 1, question: '🤔 如何避免重复？', hint: '排序', answer: '先对数组排序。排序后，相同的元素会相邻，方便去重。' },
      { step: 2, question: '📉 怎么把三数之和降维？', hint: '固定一个数', answer: '固定第一个数 nums[i]，问题就变成了在剩下的有序数组中找两个数，使得它们的和等于 -nums[i]。这就变成了“两数之和 II”。' },
      { step: 3, question: '👉 双指针怎么找两数？', hint: '左右夹逼', answer: '左指针 L = i+1，右指针 R = n-1。如果 sum > 0，R--；如果 sum < 0，L++；如果 sum == 0，记录结果并同时收缩（注意去重）。' }
    ],
    codeSteps: [
      {
        title: '排序与遍历',
        description: '先排序，再固定第一个数',
        code: `Arrays.sort(nums);
List<List<Integer>> res = new ArrayList<>();

for (int i = 0; i < nums.length; i++) {
    // 优化：如果第一个数大于0，后面不可能凑出0
    if (nums[i] > 0) break;
    
    // 去重：跳过重复的第一个数
    if (i > 0 && nums[i] == nums[i-1]) continue;
    
    // ... 双指针逻辑 ...
}`,
        explanation: '排序是基础，去重是关键。nums[i] == nums[i-1] 表示当前数字已经作为第一个数处理过了，再处理会产生重复结果。'
      },
      {
        title: '双指针寻找另外两个数',
        description: '标准的两数之和逻辑',
        code: `    int L = i + 1;
    int R = nums.length - 1;
    while (L < R) {
        int sum = nums[i] + nums[L] + nums[R];
        if (sum == 0) {
            res.add(Arrays.asList(nums[i], nums[L], nums[R]));
            // 去重：跳过重复的 L 和 R
            while (L < R && nums[L] == nums[L+1]) L++;
            while (L < R && nums[R] == nums[R-1]) R--;
            L++;
            R--;
        } else if (sum < 0) {
            L++;
        } else {
            R--;
        }
    }`,
        explanation: '找到一组解后，L 和 R 都要继续移动，同时要跳过所有重复的元素，防止产生重复的三元组。'
      }
    ],
    interview: {
      approach: '排序 + 双指针。先排序，然后遍历数组固定第一个数 nums[i]，使用双指针在 i 后面寻找和为 -nums[i] 的两个数。注意在遍历和双指针移动时都要进行去重处理。',
      timeComplexity: 'O(n²)，排序 O(nlogn)，双指针遍历 O(n²)。',
      spaceComplexity: 'O(logn) 或 O(n)，取决于排序算法的空间复杂度。',
      followUp: [
        { question: '如果是四数之和呢？', answer: '逻辑一样，多加一层循环固定第二个数，剩下两个数用双指针。复杂度 O(n³)。' }
      ]
    }
  },
  {
    id: 'trapping-rain-water',
    title: '接雨水',
    titleEn: 'Trapping Rain Water',
    category: 'two-pointers',
    difficulty: 'hard',
    description: '给定 n 个非负整数表示每个宽度为 1 的柱子的高度图，计算按此排列的柱子，下雨之后能接多少雨水。',
    examples: [
      { input: 'height = [0,1,0,2,1,0,1,3,2,1,2,1]', output: '6', explanation: '上面是由数组 [0,1,0,2,1,0,1,3,2,1,2,1] 表示的高度图，在这种情况下，可以接 6 个单位的雨水（蓝色部分表示雨水）。' },
      { input: 'height = [4,2,0,3,2,5]', output: '9' }
    ],
    thinkingGuide: [
      { step: 1, question: '💧 每个位置能接多少水？', hint: '取决于左右最高柱子', answer: '位置 i 能接的水 = min(左边最高, 右边最高) - height[i]。如果结果小于0，则为0。' },
      { step: 2, question: '🤔 怎么优化找最高柱子的过程？', hint: '双指针动态维护', answer: '使用双指针 left 和 right。维护 leftMax 和 rightMax。' },
      { step: 3, question: '⚡️ 为什么可以只看一边？', hint: '短板效应', answer: '如果 height[left] < height[right]，说明左边的 leftMax 一定小于右边的某个柱子（至少是 height[right]），所以瓶颈在左边，可以直接计算 left 处的接水量。反之亦然。' }
    ],
    codeSteps: [
      {
        title: '双指针动态计算',
        description: '一次遍历，空间 O(1)',
        code: `int left = 0, right = height.length - 1;
int leftMax = 0, rightMax = 0;
int ans = 0;

while (left < right) {
    leftMax = Math.max(leftMax, height[left]);
    rightMax = Math.max(rightMax, height[right]);

    // 哪边低，哪边就是瓶颈，就可以算哪边
    if (height[left] < height[right]) {
        ans += leftMax - height[left];
        left++;
    } else {
        ans += rightMax - height[right];
        right--;
    }
}`,
        explanation: '核心思想：对于位置 left，如果 height[left] < height[right]，那么 leftMax 一定是真实的可信瓶颈（因为右边至少有一个 height[right] 比它大，挡住了水）。'
      }
    ],
    interview: {
      approach: '双指针法。维护左右两端的最大高度 leftMax 和 rightMax。每次比较 height[left] 和 height[right]，处理较小的一端。因为接水量取决于短板，较小的一端可以确定其接水量（由该端的 Max 决定）。',
      timeComplexity: 'O(n)，一次遍历。',
      spaceComplexity: 'O(1)，只需要常数空间。',
      followUp: [
        { question: '用单调栈怎么做？', answer: '维护一个单调递减栈。遇到比栈顶高的柱子，说明形成了一个凹槽，可以计算雨水。是横向计算面积。' }
      ]
    }
  },

  // ==================== 滑动窗口类题目 ====================
  {
    id: 'sliding-window-summary',
    title: '对比总结：变长 vs 定长窗口',
    titleEn: 'Sliding Window Summary',
    category: 'sliding-window',
    difficulty: 'medium',
    description: '这两段代码分别代表了滑动窗口（Sliding Window）算法中最核心的两种形态。如果不理解它们的区别，做题时很容易搞混。\n\n简单来说：\n1. 变长窗口（while）：是为了“找最短”。像手风琴一样，先拉长，再尽可能压缩。\n2. 定长窗口（if）：是为了“找固定长度”。像一个固定大小的框，一格一格往右平移。',
    examples: [],
    thinkingGuide: [
      { step: 1, question: '🐛 变长窗口（找最短）的核心逻辑是什么？', hint: '想想毛毛虫是怎么动的', answer: '策略是“宽进严出”。右指针一直向右扩充，直到满足条件。一旦满足，立刻触发内部 while 循环，左指针开始收缩，尝试找到最小的满足条件的窗口。' },
      { step: 2, question: '🖼️ 定长窗口（找固定长度）的核心逻辑是什么？', hint: '想想相框是怎么移动的', answer: '策略是“保持体型”。窗口大小固定。每当右指针进一个，左指针必须出一个，保证窗口长度严格等于目标长度。' },
      { step: 3, question: '⚖️ 什么时候用 while，什么时候用 if？', hint: '看题目要求', answer: '找最小/最长子串用 while（需要伸缩）；找异位词/排列用 if（长度固定）。' }
    ],
    codeSteps: [
      {
        title: '变长窗口：找最短子串',
        description: '核心逻辑：while 循环压缩窗口',
        code: `// 只要窗口还是合法的（required == 0 表示所有字符都齐了），就尝试缩小
while(required == 0){ 
    // 1. 更新最小长度（因为当前是合法的，所以有机会更新结果）
    if(right - left + 1 < minLen){
        minLen = right - left + 1;
        minStart = left;
    }

    // 2. 准备移出左边的字符
    char leftChar = s.charAt(left);
    
    // 3. 先动手改数据：把字符“加回去”
    // （因为要移出窗口，所以窗口里少了，对外面的需求need就多了）
    need[leftChar]++; 
    
    // 4. 后看结果：我现在是不是欠债了？
    // 如果 need[leftChar] 变回 > 0 了，说明窗口里这个字符的数量“不够了”。
    // 于是 required++，意味着“还需要凑齐的字符种类”变多了。
    if(need[leftChar] > 0) required++;
    
    // 5. 真正移动左指针
    left++; 
}`,
        explanation: '像手风琴一样：先拉开（right++），满足条件后拼命压缩（left++），直到不能压为止。'
      },
      {
        title: '定长窗口：找异位词',
        description: '核心逻辑：if 判断维持长度',
        code: `// 1. 判断窗口是否达标：长度必须严格等于 p 的长度
if (right - left + 1 == p.length()) {
    
    // 2. 结算：如果 need 为 0（说明差异为 0，完全匹配），记录结果
    if (need == 0) res.add(left);

    // 3. 出窗口逻辑：准备移出左边的字符
    char leftChar = s.charAt(left);
    
    // 4. 先动手改数据：把字符“加回去”
    // 统一写法！先加回去，再判断状态
    count[leftChar - 'a']++;
    
    // 5. 后看结果：我现在是不是欠债了？
    // 这里的逻辑和变长窗口完全一致：先改后看
    // 只有当 count 变成正数（>0），才说明我们真的缺这个字符了
    if (count[leftChar - 'a'] > 0) need++;
    
    // 6. 左指针移动（平移）
    left++;
}`,
        explanation: '像相框平移：右边进一个，左边必须出一个，永远保持固定大小。'
      },
      {
        title: '深度解析：欠债模型（>0 vs >=0）',
        description: '为什么强烈建议用“先改后看”的写法？',
        code: `// 核心区别：你是先“看一眼”再动手，还是先“动手”再看一眼？

// 场景：假设 count['a'] = 0 (刚好收支平衡)

// 写法 1：先看后改 (不推荐，容易精神分裂)
// if (count['a'] >= 0) need++; // 此时还是0，所以要判断 >= 0
// count['a']++; // 然后才改数据

// 写法 2：先改后看 (推荐！符合直觉)
// count['a']++; // 先动手！0 变成了 1
// if (count['a'] > 0) need++; // 此时已经是1了，所以判断 > 0

// 总结：
// 1. 正数 (>0) = 欠债
// 2. 零 (0) = 平衡
// 3. 负数 (<0) = 富余 (或者无关字符)

// 只要记住“先改后看”，所有判断条件统一用 > 0，逻辑瞬间清晰！`,
        explanation: '把字符计数想象成“欠债”。正数表示欠债，0表示平衡，负数表示富余。无关字符永远在0和负数之间变化，永远不会变成正数（欠债），所以永远不会触发 need 变化。'
      }
    ],
    interview: {
      approach: '理解这两种形态的区别是掌握滑动窗口的关键。变长窗口用于求极值（最小覆盖、最长无重复），定长窗口用于求固定长度的子串（异位词、排列）。',
      timeComplexity: '两者都是 O(n)，因为左右指针都只遍历一次数组。',
      spaceComplexity: 'O(1) 或 O(k)，取决于字符集大小。',
      followUp: [
        { question: '如果要求“至多包含 k 个不同字符的最长子串”，用哪种？', answer: '变长窗口。因为窗口大小不固定，需要通过收缩左边界来满足“至多 k 个”的条件。' },
        { question: '如果要求“长度为 k 且和最大的子数组”，用哪种？', answer: '定长窗口。因为长度固定为 k。' }
      ]
    }
  },
  {
    id: 'minimum-window-substring',
    title: '最小覆盖子串',
    titleEn: 'Minimum Window Substring',
    category: 'sliding-window',
    difficulty: 'hard',
    description: '给你一个字符串 s 、一个字符串 t 。返回 s 中涵盖 t 所有字符的最小子串。如果 s 中不存在涵盖 t 所有字符的子串，则返回空字符串 "" 。',
    examples: [
      { input: 's = "ADOBECODEBANC", t = "ABC"', output: '"BANC"', explanation: '最小覆盖子串 "BANC" 包含 A, B, C。' },
      { input: 's = "a", t = "a"', output: '"a"' },
      { input: 's = "a", t = "aa"', output: '""' }
    ],
    thinkingGuide: [
      { step: 1, question: '🤔 怎么判断覆盖了？', hint: '字符计数', answer: '用一个数组或哈希表统计 t 中每个字符出现的次数。' },
      { step: 2, question: '🐛 窗口怎么动？', hint: '右扩左缩', answer: '右指针不断向右扩展，直到窗口包含了 t 的所有字符。然后左指针向右收缩，尝试找到最小的窗口。' },
      { step: 3, question: '⚡️ 怎么快速判断是否全覆盖？', hint: '计数器', answer: '维护一个变量 required，记录还需要凑齐的字符种类数。当 required == 0 时，说明全覆盖了。' }
    ],
    codeSteps: [
      {
        title: '初始化',
        description: '统计 t 的字符频率',
        code: `int[] need = new int[128];
for (char c : t.toCharArray()) need[c]++;
int required = 0; // 统计有多少种字符需要凑（注意是种类，不是总数，或者用总数也行，看具体实现）
// 这里为了简单，我们用总数逻辑，或者用种类逻辑。
// 推荐：用总数逻辑（need > 0 的字符总数）更直观，或者用种类逻辑（need[c] > 0 的字符个数）。
// 让我们用最通用的“种类”逻辑：
int count = t.length(); // 需要凑齐的总字符数`,
        explanation: '准备工作：知道我们需要什么，以及需要多少。'
      },
      {
        title: '滑动窗口',
        description: '右进左出',
        code: `int left = 0, right = 0;
int minLen = Integer.MAX_VALUE, start = 0;
int[] window = new int[128]; // 也可以直接复用 need 数组，做减法

// 复用 need 数组做减法更简洁：
// need[c] > 0 表示还需要 c
// need[c] <= 0 表示 c 够了或者多了

for (right = 0; right < s.length(); right++) {
    char c = s.charAt(right);
    // 1. 进窗口：如果是需要的字符，count 减 1
    if (need[c] > 0) count--;
    need[c]--; // 无论是不是需要的，都减 1（负数表示多余）

    // 2. 满足条件：count == 0，说明所有字符都齐了
    while (count == 0) {
        // 更新最小覆盖子串
        if (right - left + 1 < minLen) {
            minLen = right - left + 1;
            start = left;
        }

        // 3. 出窗口
        char d = s.charAt(left);
        left++;
        
        // 核心逻辑：先改后看
        need[d]++; // 先加回去
        if (need[d] > 0) count++; // 如果变回正数，说明欠债了
    }
}`,
        explanation: '核心逻辑：右指针负责找齐所有字符，左指针负责在找齐的基础上尽可能缩小窗口。'
      }
    ],
    interview: {
      approach: '滑动窗口。维护一个 need 数组记录需要的字符。右指针扩展，遇到目标字符就减少需求。当需求满足时，左指针收缩，更新最小长度。',
      timeComplexity: 'O(n)，左右指针各遍历一次。',
      spaceComplexity: 'O(1)，字符集大小固定（128）。',
      followUp: [
        { question: '如果字符集很大怎么办？', answer: '用 HashMap 代替数组。' }
      ]
    }
  },
  {
    id: 'find-all-anagrams-in-a-string',
    title: '找到字符串中所有字母异位词',
    titleEn: 'Find All Anagrams in a String',
    category: 'sliding-window',
    difficulty: 'medium',
    description: '给定两个字符串 s 和 p，找到 s 中所有是 p 的 字母异位词 的子串，返回这些子串的起始索引。',
    examples: [
      { input: 's = "cbaebabacd", p = "abc"', output: '[0,6]', explanation: '起始索引等于 0 的子串是 "cba", 它是 "abc" 的异位词。\n起始索引等于 6 的子串是 "bac", 它是 "abc" 的异位词。' },
      { input: 's = "abab", p = "ab"', output: '[0,1,2]' }
    ],
    thinkingGuide: [
      { step: 1, question: '🤔 异位词意味着什么？', hint: '长度相同，字符相同', answer: '窗口长度必须严格等于 p.length()，且窗口内字符计数与 p 一致。' },
      { step: 2, question: '🖼️ 窗口大小是固定的吗？', hint: '是的', answer: '是的，这是一个定长窗口问题。窗口大小固定为 p.length()。' },
      { step: 3, question: '⚡️ 怎么优化比较过程？', hint: '维护差异', answer: '不需要每次都比较两个 Map。维护一个 need 变量，表示还需要凑齐的字符数量。' }
    ],
    codeSteps: [
      {
        title: '初始化与统计',
        description: '统计 p 的字符频率',
        code: `List<Integer> res = new ArrayList<>();
if (s.length() < p.length()) return res;

int[] count = new int[26];
for (char c : p.toCharArray()) count[c - 'a']++;

int need = p.length(); // 需要凑齐的字符总数
int left = 0;`,
        explanation: 'count 数组记录每个字符“欠”多少个。need 记录总共“欠”多少个字符。'
      },
      {
        title: '右指针扩张',
        description: '进窗口逻辑',
        code: `for (int right = 0; right < s.length(); right++) {
    char rightChar = s.charAt(right);
    
    // 1. 进窗口：如果是需要的字符，need 减 1
    if (count[rightChar - 'a'] > 0) {
        need--;
    }
    // 无论如何，计数减 1（负数表示多余）
    count[rightChar - 'a']--;
    
    // ... 后续逻辑 ...
}`,
        explanation: '右指针向右移动，把字符加入窗口。如果这个字符是我们需要的（count > 0），那么总需求 need 就减少。'
      },
      {
        title: '完整实现',
        description: '定长窗口的维护',
        code: `    // ... 接上面 ...
    
    // 2. 检查窗口大小
    if (right - left + 1 == p.length()) {
        // 如果 need 为 0，说明完全匹配
        if (need == 0) res.add(left);

        // 3. 出窗口
        char leftChar = s.charAt(left);
        
        // 核心逻辑：先改后看
        count[leftChar - 'a']++; // 先加回去
        if (count[leftChar - 'a'] > 0) { // 如果变回正数，说明欠债了
            need++;
        }
        left++;
    }
}`,
        explanation: '当窗口大小达到 p.length() 时，检查是否满足条件。然后移动左指针，保持窗口大小不变。注意出窗口时的“先改后看”逻辑。'
      }
    ],
    interview: {
      approach: '定长滑动窗口。维护一个大小为 p.length() 的窗口。使用数组记录字符差异。每次右边进一个，左边出一个，实时更新差异计数。',
      timeComplexity: 'O(n)，一次遍历。',
      spaceComplexity: 'O(1)，字符集大小固定（26）。',
      followUp: [
        { question: '如果字符集包含 Unicode 呢？', answer: '使用 HashMap 代替数组。' }
      ]
    }
  },
  {
    id: 'longest-substring-without-repeating-characters',
    title: '无重复字符的最长子串',
    titleEn: 'Longest Substring Without Repeating Characters',
    category: 'sliding-window',
    difficulty: 'medium',
    description: '给定一个字符串 s ，请你找出其中不含有重复字符的 最长子串 的长度。',
    examples: [
      { input: 's = "abcabcbb"', output: '3', explanation: '因为无重复字符的最长子串是 "abc"，所以其长度为 3。' },
      { input: 's = "bbbbb"', output: '1', explanation: '因为无重复字符的最长子串是 "b"，所以其长度为 1。' },
      { input: 's = "pwwkew"', output: '3', explanation: '因为无重复字符的最长子串是 "wke"，所以其长度为 3。' }
    ],
    thinkingGuide: [
      { step: 1, question: '🤔 怎么判断重复？', hint: '哈希表', answer: '用哈希表或数组记录窗口内每个字符出现的次数（或上一次出现的位置）。' },
      { step: 2, question: '🐛 遇到重复怎么办？', hint: '左指针收缩', answer: '如果 right 指向的字符已经存在于窗口中，说明有重复。此时 left 指针需要向右移动，直到把那个重复的字符移出窗口为止。' },
      { step: 3, question: '📏 什么时候更新最大长度？', hint: '每次移动后', answer: '每次 right 移动并处理完重复后，当前窗口 [left, right] 一定是合法的，此时更新 maxLen。' }
    ],
    codeSteps: [
      {
        title: '滑动窗口',
        description: '使用 Set 或 Map 判重',
        code: `int left = 0, right = 0;
int maxLen = 0;
Set<Character> window = new HashSet<>();

while (right < s.length()) {
    char c = s.charAt(right);
    // 如果有重复，左指针一直缩，直到重复字符被移出
    while (window.contains(c)) {
        window.remove(s.charAt(left));
        left++;
    }
    // 加入当前字符
    window.add(c);
    // 更新最大长度
    maxLen = Math.max(maxLen, right - left + 1);
    right++;
}`,
        explanation: '这是最直观的写法。只要有重复，左边就一直删。保证窗口内永远没有重复字符。'
      }
    ],
    interview: {
      approach: '滑动窗口。使用 Set 记录窗口内的字符。右指针扩展，遇到重复字符时，左指针收缩直到重复字符被移除。每次扩展都更新最大长度。',
      timeComplexity: 'O(n)，每个字符最多进出窗口各一次。',
      spaceComplexity: 'O(1)，字符集大小固定。',
      followUp: [
        { question: '能优化吗？', answer: '可以用 Map 记录字符索引。遇到重复时，直接把 left 跳到重复字符的下一位，避免一步步移动。' }
      ]
    }
  }
];

export const categories = [
  { id: 'hash-table', title: '哈希表', icon: 'Hashtag' },
  { id: 'two-pointers', title: '双指针', icon: 'MousePointer2' },
  { id: 'sliding-window', title: '滑动窗口', icon: 'Maximize' },
  { id: 'linked-list', title: '链表', icon: 'Link' },
  { id: 'binary-tree', title: '二叉树', icon: 'GitFork' },
  { id: 'backtracking', title: '回溯算法', icon: 'Undo2' },
  { id: 'dynamic-programming', title: '动态规划', icon: 'TrendingUp' },
  { id: 'greedy', title: '贪心算法', icon: 'Target' }
];

export const allProblems = problems;
